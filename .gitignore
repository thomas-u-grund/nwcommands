# Derived/regenerable test artifacts (regenerated from cscripts/ automatically)
lib/cscripts_prod/
lib/*.log
*.log

# Editor/OS
.DS_Store

# GitHub migration (2026-08-23): excluded from the public repo per an
# explicit decision, not an oversight - see docs/CERTIFICATION.md's
# migration-unit entry and the migration notes in docs/ROADMAP.md.
# StataBook/: a defunct, superseded draft of the author's book manuscript
# (LaTeX source, PDFs, figures) - confirmed page-count-identical to
# snapshots from 2015 and 2022 found elsewhere on disk, i.e. abandoned
# long before this session, not something broken by it. The current
# manuscript lives in book/ instead. Archived under deprecated/StataBook/
# rather than deleted outright; not referenced by any live .ado command,
# and possibly under an active publisher agreement, so kept out of the
# public repo by default rather than assumed clear to publish.
StataBook/
# TODO/: personal working notes (a .docx to-do list plus its own Word
# lock/temp file), not project documentation.
TODO/
# A stale, fully-redundant historical snapshot (a 2022 zip of an old
# project state, with __MACOSX resource-fork junk from how it was
# created) - everything genuinely current is already tracked directly;
# this duplicate only adds repository bloat.
deprecated/nwcommands_2016.zip
.Rhistory
*.swp
.swp
docs/

# Local-only project/dev files (housekeeping 2026-09-05): kept on disk for
# the author's own reference but not published to the public GitHub repo.
# Purged from git history at the same time (git filter-repo), not just
# untracked going forward.
CLAUDE.md
LEGACY_FILES.md
*.R
deprecated/
dev/
old/

# Jekyll build output (website/) - regenerated by the deploy workflow, not committed
website/_site/
website/.jekyll-cache/
website/.jekyll-metadata
website/Gemfile.lock
website/.bundle/
website/vendor/
